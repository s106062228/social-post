import { AccountComparisonClient } from "./account-comparison-client";

export const metadata = {
  title: "Compare Accounts — PostFlow",
};

export default function AccountComparisonPage() {
  return <AccountComparisonClient />;
}
